# OCR 전용 의존성 — worker 컨테이너에서만 설치.
# torch / torchvision 은 CUDA wheel 인덱스에서 별도 설치 (Dockerfile 참조).
#
# ⚠ 버전 핀 주의:
#  - surya-ocr 0.17.x 는 transformers 4.x 전용 (5.x 에서 PreTrainedModel import 경로 변경으로 깨짐).
#  - 상한 미지정 시 pip 가 transformers 5.x 를 끌어와서 런타임 ImportError 발생.
surya-ocr==0.17.1
transformers>=4.45,<5.0
